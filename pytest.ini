[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    browser: requires Playwright chromium (skipped when browsers not installed)
    network: requires live network (skipped by default)
addopts = -m "not browser and not network"
