# Glider v3.2 Usage Phase

Status: complete. Release commit `17ffd39dfc8698738a7c27e3cf80b46369e8faad`
and annotated tag `v3.2.0` are pushed; final documentation commit `71881f2`
is on `origin/main`.

## Completed scope

- [x] Validate real usage patterns with a deterministic local HTTP server.
- [x] Cover HTML pagination and extraction through the real HTTP transport.
- [x] Cover JSON API extraction, nested links, and list-mode concurrency.
- [x] Add a repeatable benchmark command and usage documentation.
- [x] Add list, API POST, and nested-page configuration templates.
- [x] Validate every checked-in example in the test suite.
- [x] Perform a focused code review of fixture lifecycle, thread-safe request
  accounting, temporary benchmark artifacts, policy exceptions, and docs.
- [x] Fix the review finding around concurrent fixture request accounting.

## Verification

- `venv\Scripts\python.exe -m pytest tests -q`: 313 passed, 1 deselected.
- `npx --yes pyright --pythonpath .\venv\Scripts\python.exe engine main.py verify_release.py benchmarks`:
  0 errors, 0 warnings, 0 informations.
- `venv\Scripts\python.exe -m compileall -q engine benchmarks main.py verify_release.py`:
  passed.
- All three example JSON files validate with the CLI.
- Local benchmark baseline: 100 pages, concurrency 10, three runs,
  approximately 49.9–52.0 requests/second, zero failures.
